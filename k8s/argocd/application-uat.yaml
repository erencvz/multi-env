# Argo CD Application manifest for the uat environment — manual sync for targeted deploys.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: multi-env-uat
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/erencvz/multi-env.git
    targetRevision: HEAD
    path: k8s/overlays/uat
  destination:
    server: https://167.86.118.166:6443
    namespace: multi-env-uat
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
